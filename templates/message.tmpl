{{ define "head" }}
  <title>{{ .Context.Title }} | HackSoc</title>
  <link rel="stylesheet" type="text/css" href="assets/css/message.min.css" />
{{ end }}


{{ define "content" }}
  <section id="message">
    <h1>{{ .Context.Title }}</h1>
    <div class="response">
      <p>{{ .Context.Message }}</p>
    </div>
  </section>
{{ end }}